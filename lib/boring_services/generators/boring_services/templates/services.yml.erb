# Infrastructure Services Configuration
# Generated by boring_services
#
# 💡 Either:
#   1. Edit this file manually with your service hosts
#   2. Use Terraform to auto-generate this file
#
# See: config/services.example.yml for full examples

shared: &shared
  user: ubuntu
  ssh_key: ~/.ssh/id_rsa

development:
  <<: *shared
  services:
    - name: redis
      enabled: true
      host: localhost
      port: 6379
      memory_mb: 256

production:
  <<: *shared
  services:
    # TODO: Add your services here
    # - name: redis
    #   enabled: true
    #   host: YOUR_SERVER_IP
    #   port: 6379
    #   memory_mb: 1024
    #
    # - name: haproxy
    #   enabled: true
    #   host: YOUR_LB_IP
    #   port: 80
    #   backends:
    #     - host: YOUR_APP_IP_1
    #       port: 3000

  secrets:
    redis_password: $REDIS_PASSWORD

